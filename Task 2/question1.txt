
  i) .   c. int multiply(int a, int b) => a * b;
  ii).   2. Async functions
  iii).  3. List
  iv).   1. The output is –‘WORD’
  v).    3. print(‘Your name is ‘{name}’);
  vi).   2. nums.forEach(print);
  vii).  1. Ensure type safety
	   2. Reduce code duplication
	   4. Can work in a single interface that takes a type parameter
